@model List<LogicaCompartida.Entidades.Definir_Compras.Compras>
@{
    ViewData["Title"] = "Gestión de Compras";
}

<div class="container-fluid">
    <div class="row">
        <div class="col-12">
            <h2>@ViewData["Title"]</h2>

            @if (ViewBag.Error != null)
            {
                <div class="alert alert-danger">@ViewBag.Error</div>
            }

            @if (TempData["Success"] != null)
            {
                <div class="alert alert-success">@TempData["Success"]</div>
            }

            @if (TempData["Error"] != null)
            {
                <div class="alert alert-danger">@TempData["Error"]</div>
            }

            <div class="row mb-3">
                <div class="col-md-6">
                    <a href="@Url.Action("Create")" class="btn btn-primary">
                        <i class="fas fa-plus"></i> Nueva Compra
                    </a>
                </div>
                <div class="col-md-6">
                    <form method="get" action="@Url.Action("Search")" class="d-flex">
                        <input type="text" name="searchTerm" class="form-control me-2" placeholder="Buscar Compra..." 
                               value="@ViewBag.SearchTerm">
                        <button type="submit" class="btn btn-outline-secondary">Buscar</button>
                        @if (!string.IsNullOrEmpty(ViewBag.SearchTerm))
                        {
                            <a href="@Url.Action("Index")" class="btn btn-outline-secondary ms-2">Limpiar</a>
                        }
                    </form>
                </div>
            </div>

            <div class="card">
                <div class="card-body">
                    @if (Model.Any())
                    {
                        <div class="table-responsive">
                            <table class="table table-hover">
                                <thead class="table-dark">
                                    <tr>
                                        <th>ID Compra</th>
                                        <th>ID Proveedor</th>
                                        <th>Fecha Compra</th>
                                        <th>ID Medicamento</th>
                                        <th>Cantidad Comprada</th>
                                        <th>Precio Unidad</th>
                                        <th>ID Empleado</th>
                                        <th class="text-center">Acciones</th>
                                    </tr>
                                </thead>
                                <tbody>
                                    @foreach (var compras in Model)
                                    {
                                        <tr>
                                            <td>@compras.IdCompra</td>
                                            <td>@compras.IdProveedor</td>
                                            <td>@compras.FechaCompra</td>
                                            <td>@compras.IdMedicamento</td>
                                            <td>@compras.CantidadComprada</td>
                                            <td>@compras.PrecioUnidad</td>
                                            <td>@compras.</td>
                                            <td class="text-center">
                                                <a href="@Url.Action("Edit", new { id = compras.IdCompra })" 
                                                   class="btn btn-sm btn-warning me-1">
                                                    <i class="fas fa-edit"></i> Editar
                                                </a>
                                                <a href="@Url.Action("Delete", new { id = compras.IdCompra })" 
                                                   class="btn btn-sm btn-danger"
                                                   onclick="return confirm('¿Está seguro de eliminar esta compra?')">
                                                    <i class="fas fa-trash"></i> Eliminar
                                                </a>
                                            </td>
                                        </tr>
                                    }
                                </tbody>
                            </table>
                        </div>
                    }
                    else
                    {
                        <div class="text-center">
                            <p class="text-muted">No se encontraron compras.</p>
                        </div>
                    }
                </div>
            </div>
        </div>
    </div>
</div>